import instance from "../utils/axiosCustomize";

const postLoginParents = (username, password) => {};
const postRegisterParents = (username, password, CCCD) => {};

export { postLoginParents, postRegisterParents };
